export type StellarParams = {
    address: string;
    lastTransactionHash?: string;
};
export type StellarRequestParams = {
    address: string;
    lastTransactionHash?: string;
    cursor?: string;
};
